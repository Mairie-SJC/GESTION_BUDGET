<form method="post" action="{{ path('app_contrat_licence_delete', {'id': contrat_licence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat_licence.id) }}">
    <button class="btn">Delete</button>
</form>
